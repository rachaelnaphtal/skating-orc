# macOS
.DS_Store

# Streamlit: track ``.streamlit/config.toml`` in git; keep secrets local
.streamlit/secrets.toml

# Local TLS certs / keys (e.g. dev HTTPS)
ssl/

# Archived / legacy code not part of the main app
archive/

# Database dumps
backups/

# Python
__pycache__/
# Local venv metadata (repo-level or copied venv)
pyvenv.cfg

activityAnalysis/__pycache__

# Microsoft Office lock/temp files while a document is open (e.g. ~$US_SYS_Sectionals.xlsx)
**/~$*.xls*

analysisTemp/